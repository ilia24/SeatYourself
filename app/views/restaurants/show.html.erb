
<p>
  <%= link_to 'Back to all restaurants', restaurants_path %>
</p>

<%= render 'single_restaurant_view', restaurant: @restaurant %>

<!-- <div class = "capacity" >
  <p>
        Restaurant capacity: <%= @restaurant.capacity %>
        Price range: <%= Restaurant.price_range_conversion(@restaurant.id)[0] %> <%= Restaurant.price_range_conversion(@restaurant.id)[1] %>
  </p>
</div> -->


<!-- <div class= "reviews">
<h3> Reviews </h3>
<p> Reviews for <%= @restaurant.name %>
  <% @restaurant.reviews.each do |review| %>
  <p>
      <%= review.comment %>
      <br>
      <% if review.user != nil %>
       <em> by <%=review.user.name %> </em>
       Added on: <%= review.created_at.strftime("%Y-%m-%d %I:%M %p") %>
       <%end %>
    </p>
  <%end%>

  <% if current_user %>
<h4> New Review </h4>

<%= form_for([@restaurant, @review]) do |f| %>

<% if @review.errors.any? %>
     <div id="error_explanation">
       <h2><%= pluralize(@review.errors.count, "error") %> prohibited this review from being saved:</h2>

       <ul>
       <% @review.errors.full_messages.each do |msg| %>
         <li><%= msg %></li>
       <% end %>
       </ul>
     </div>
   <% end %>

    <p>
    <%= f.label :comment %>
    <%= f.text_area :comment %>
    </p>
    <p>
    <%= f.submit %>
    </p>
    <% end %>

    <% else %>
    <p> Please <%= link_to 'Log In', new_session_path %> or <%= link_to 'Sign Up', new_user_path%> to add a review
    </p>
      <% end %>
    </div> -->
